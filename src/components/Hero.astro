<section id="home" class="relative overflow-hidden mb-12">

  <!-- Desktop: Split Layout -->
  <div class="hidden lg:grid grid-cols-2 max-w-7xl mx-auto">

    <!-- Left: Video -->
    <div class="relative w-full h-[92vh] overflow-hidden">
      <video
        src="/video/eKoneni-Lounge-Background-video.mp4"
        autoplay
        muted
        loop
        playsinline
        preload="metadata"
        class="absolute inset-0 w-full h-full object-cover"
      ></video>
      <div class="absolute inset-0 bg-black/50"></div>
    </div>

    <!-- Right: Text + Buttons -->
    <div class="flex items-center px-10 lg:pl-16 bg-[#0d0a05]">
      <div class="max-w-xl">

        <!-- Location Tag -->
        <div class="inline-flex items-center gap-2 rounded-full border border-amber-400/30 bg-black/40 px-4 py-1.5 mb-6
                    opacity-0 animate-[fadeSlide_0.6s_ease_forwards]">
          <span class="w-2 h-2 rounded-full bg-amber-400 animate-pulse"></span>
          <span class="font-body text-amber-300 tracking-[0.2em] uppercase text-xs sm:text-sm">
            Mdantsane · East London · Est. 2019
          </span>
        </div>

        <!-- Headline -->
        <h1 class="font-display font-black text-white leading-none tracking-tighter mb-5
                   opacity-0 animate-[fadeSlide_0.7s_ease_forwards_0.1s]">
          <span class="block text-6xl">Where Mdantsane</span>
          <span class="block text-6xl text-amber-400">Truly Comes Alive.</span>
        </h1>

        <!-- Subheadline -->
        <p class="font-body text-gray-300 text-xl max-w-md mb-9 leading-relaxed
                  opacity-0 animate-[fadeSlide_0.7s_ease_forwards_0.3s]">
          Premium amapiano nights, unforgettable energy, and a community that feels like family.
        </p>

        <!-- Buttons -->
        <div class="flex gap-4 opacity-0 animate-[fadeSlide_0.7s_ease_forwards_0.5s]">
          <a href="#contact" class="btn-gold px-9 py-3.5 rounded-full text-base font-medium tracking-wide">
            Contact Us
          </a>
          <a href="#events" class="btn-outline px-9 py-3.5 rounded-full text-base font-medium tracking-wide">
            See Upcoming Events
          </a>
        </div>

      </div>
    </div>

  </div>

  <!-- Mobile: Improved Stacked Layout -->
  <div class="block lg:hidden">

    <!-- Mobile Video -->
    <div class="w-full aspect-video relative">
      <video
        src="/video/eKoneni-Lounge-Background-video.mp4"
        autoplay
        muted
        loop
        playsinline
        preload="metadata"
        class="w-full h-full object-cover"
      ></video>
      <div class="absolute inset-0 bg-black/55"></div>
    </div>

    <!-- Mobile Content -->
    <div class="px-5 pt-8 pb-10">
      
      <!-- Location Tag -->
      <div class="inline-flex items-center gap-2 rounded-full border border-amber-400/25 bg-amber-400/5 px-3 py-1 mb-5">
        <span class="w-1.5 h-1.5 rounded-full bg-amber-400 animate-pulse"></span>
        <span class="font-body text-amber-300/90 tracking-[0.2em] uppercase text-xs">
          Mdantsane · East London · Est. 2019
        </span>
      </div>

      <!-- Headline -->
      <h1 class="font-display font-bold text-white text-4xl leading-tight mb-4">
        Where Mdantsane <span class="text-amber-400">Truly Comes Alive.</span>
      </h1>

      <!-- Subheadline -->
      <p class="font-body text-gray-300 text-base mb-8 max-w-md">
        Premium amapiano nights, unforgettable energy, and a community that feels like family.
      </p>

      <!-- Buttons -->
      <div class="flex flex-col gap-3">
        <a href="#contact" class="btn-gold w-full text-center px-8 py-3.5 rounded-full text-sm font-medium tracking-wide">
          Contact Us
        </a>
        <a href="#events" class="btn-outline w-full text-center px-8 py-3.5 rounded-full text-sm font-medium tracking-wide">
          See Upcoming Events
        </a>
      </div>

    </div>
  </div>

</section>

<style>
  @keyframes fadeSlide {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>